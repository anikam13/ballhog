// Public feedback / contact channel (Tally). Used in footer, Settings, Terms, Privacy.
export const FEEDBACK_FORM_URL = "https://tally.so/r/VLqdDg";

/**
 * Optional analytics IDs — set in deploy env only when ready.
 * See src/analytics.ts. Leave unset to ship with no pixels.
 *
 * VITE_GA4_MEASUREMENT_ID
 * VITE_META_PIXEL_ID
 */
